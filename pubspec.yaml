name: projeto_finan
description: Aplicativo de controle financeiro

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  #assets:
   # - assets/images/waiting.png  # Certifique-se de criar esta pasta e adicionar a imagem